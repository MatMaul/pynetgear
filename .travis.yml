language: python
os: linux

before_install:
  - pip install flake8

jobs:
  include:
    - stage: lint
      python: 3.7
      script: flake8 pynetgear
    - stage: test
      python: 3.7
      script: python test/test_getAttachedDevices.py

stages:
  - lint
  - test
